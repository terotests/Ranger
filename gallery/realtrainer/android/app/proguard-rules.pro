# Nothing in the page is reached reflectively; these are here so a release build
# that turns minification on has a starting point rather than a surprise.
-keep class fi.ranger.realtrainer.MainActivity { *; }
