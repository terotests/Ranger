# The page is one generated Kotlin file of ~87k lines. kotlinc wants room for
# it — the default 1g heap runs out somewhere in the middle of the layout
# engine.
org.gradle.jvmargs=-Xmx8g -XX:MaxMetaspaceSize=1g
org.gradle.parallel=true
android.useAndroidX=false
kotlin.code.style=official
