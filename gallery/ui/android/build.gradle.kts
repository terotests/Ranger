// Versions are declared here and applied in app/build.gradle.kts so there is
// one place to move them. Nothing else lives at the root.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
