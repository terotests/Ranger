{
  "name": "input_password",
  "component": "input",
  "behaviours": ["input-password", "input-type"],
  "$comment": "The VALUE is the text. What gets drawn is bullets, and the trace cannot see drawing — so this half of the contract is measured here and the other half in the demo gate, which reads the display list and asserts the two strings are never the same.",
  "fixture": { "controls": [{ "type": "input", "tid": "pw", "kind": "password", "name": "Password" }] },
  "steps": [
    { "focus": "pw" },
    { "type": "s3cret" },
    { "key": "Backspace" },
    { "key": "Home" },
    { "key": "Delete" }
  ]
}
