import SwiftUI

/// Imaginary app entry — in Xcode: `@main struct CounterBoardApp: App { … }`
enum CounterBoardAppLauncher {
    static func rootView() -> some View {
        CounterBoardView()
    }
}
