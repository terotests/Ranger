# The viewer is one generated Kotlin file of ~66k lines. kotlinc wants room for
# it — the default 1g heap runs out somewhere in the middle of the OOXML parser.
org.gradle.jvmargs=-Xmx6g -XX:MaxMetaspaceSize=1g
org.gradle.parallel=true
android.useAndroidX=false
kotlin.code.style=official
