# The viewer, compiled from Ranger. Two megabytes of generated Kotlin, rebuilt
# by scripts/build-ranger.sh — a checked-in copy is the one way this port can
# drift from the source everything else runs.
generated/*.kt

# Copies of fonts and a deck that already live in this repository.
app/src/main/assets/

# Gradle
.gradle/
build/
app/build/
local.properties
