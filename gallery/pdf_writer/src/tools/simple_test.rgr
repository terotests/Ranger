; Simple test of cross-directory import
Import "../../../evg/EVGElement.rgr"

class SimpleTest {
    def element:EVGElement
    
    Constructor () {
        element = (new EVGElement())
    }
    
    fn run:void () {
        print "SimpleTest works!"
    }
    
    sfn m@(main):void () {
        def t (new SimpleTest())
        t.run()
    }
}
