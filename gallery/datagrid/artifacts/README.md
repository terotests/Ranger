# DataGrid walkthrough screenshots

Generated by `npm run datagrid:artifacts` (SoftCanvas → PNGEncoder).

| File | What it shows |
| --- | --- |
| `01_business_summary_cf.png` / `.jpg` | Summary KPIs + color-scale conditional formatting |
| `02_sales_filter_sort_menu.png` / `.jpg` | Sales sheet with SheetView filter/sort + header popup |
| `03_formulas_calc.png` / `.jpg` | formulas.xlsx Calc sheet |
| `04_styles_showcase.png` | the border / font / number-format matrix |
| `05_paste_special.png` | Paste Special, on the shared EVG window layer |
| `06_chart_picker.png` | the chart picker: twenty types, six styles, and the chart previewed live beside them |
| `07_charts_floating.png` | two charts floating over the sheet, drawn by Vela |
| `08_text_rotation.png` | OOXML text rotation: 45° up, 90° up, 45° down, stacked |
| `09_find_replace.png` | find and replace, with the window layer's text fields |
| `10_cf_rule_editor.png` | writing a conditional-formatting rule, not just reading one |
| `11_disjoint_selection.png` | two Ctrl+click rectangles, over a rule's green fill |
| `12_links_notes.png` | hyperlinks painted as links, a note marker and the open note |
| `13_validation_list.png` | a list rule offering its choices |
| `14_rich_text.png` | several styles inside one cell, sharing a baseline |
| `15_images.png` | two anchored pictures — one stretched between cells, one pinned |
| `16_custom_tools.png` | a host's own tools on the status bar, beside the built-in one |
| `17_chart_gallery.png` | six of the twenty chart types, each in a different style, from one table |
| `18_chart_copy.png` | a chart window with its Copy button |
| `18_clipboard_flavour.png` | the exact bytes the clipboard carries as `image/png` |
| `18_clipboard_flavour.svg` | …and as `image/svg+xml`, from the same render |
| `18_clipboard_flavour.html` | …and as `text/html`, the flavour Word and Excel take |
| `19_browser_only.png` | the editor running in a browser with **no host** — WebGL, real workbook, `npm run datagrid:web:test -- --shot` |
| `20_pptx_browser_only.png` | the slide viewer, same deal — `npm run pptx:web:test -- --shot` |
| `21_docx_browser_only.png` | the Word viewer drawing a page with WebGL instead of shipping a PNG — `npm run docx_viewer:web:test -- --shot` |

These are tracked (see `.gitignore` exceptions) so PR review can see real images.
`tools/check_png.py` reads them back with Python's `zlib` — signature, chunk
CRCs, inflate, scanline filters — so a change to the PNG writer cannot quietly
produce files that only our own decoder accepts.
