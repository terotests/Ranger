Snapshots from `bash gallery/friendly/rust/compile.sh`.

Each `0N_*.rs` is what `-l=rust -strict-ownership` wrote for the matching
file in `../src/`. Each `0N_*.out` is what the `rustc` binary printed.
Re-run the script after a compiler change to refresh them.
