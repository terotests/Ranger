[package]
name = "ylos3d_wasm"
version = "0.0.0"
edition = "2021"

[lib]
name = "logic"
crate-type = ["cdylib"]

[dependencies]
libm = "0.2"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
