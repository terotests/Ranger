# three — staged Ranger Three.js port

Container for the modular Three port copied from v1. Live host-backed objects
eventually live under `host/arenas/three` + `modules/ranger_three`; this tree
preserves the well-tested class-per-file port and its unit tests.

See [`port/`](./port/).
