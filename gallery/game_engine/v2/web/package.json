{
  "name": "ranger-web-games",
  "private": true,
  "version": "1.0.0",
  "description": "Ranger game engine compiled to JS, running games in the browser with a Monaco editor + live reload.",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "monaco-editor": "0.55.1"
  },
  "overrides": {
    "dompurify": "^3.4.13"
  }
}
