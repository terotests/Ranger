# ui — staged retained UI / EVG launcher widgets

**Copied from:** `gallery/game_engine/ui/`.

UILayer, widgets, EVG launcher menu, WASM UI helpers. Pairs with `v2/evg/` and
RGU1 (`bridge/wasm/legacy_blocks/wasm_ui_abi.h`).

**Plan phase:** after modules; optional for early 3D bridge gates.

## Unit / contract tests that gate this folder

- Existing `ui/tests` and demos (rewire paths as needed)
