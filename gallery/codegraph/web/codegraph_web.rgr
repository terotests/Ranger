; SPDX-License-Identifier: AGPL-3.0-or-later

; ============================================================================
; codegraph_web.rgr — browser entry for the Full EVG explorer.
; ============================================================================
;
; The page still looks for `CodeGraphWeb`. The explorer itself lives in
; `CodeGraphApp`: one EvgApp for the tab and for the SDL2 window.
; ============================================================================

Import "../src/CodeGraphApp.rgr"

class CodeGraphWeb extends CodeGraphApp {
    Constructor () {
    }
}
