# Generated Kotlin

Run from gallery root:

```bash
npm run build:ranger
```

Produces `counter_board.kt` (Ranger `@process` runtime + board). Copy into your Android module or add as a source root after reviewing [ISSUES.md](../ISSUES.md).
