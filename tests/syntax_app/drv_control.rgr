; Driver: compiles and runs the `control` section on its own, so that a target
; that rejects one section does not hide the others.
Import "sx_control.rgr"

class ControlDriver {
    sfn m@(main):void () {
        def s:SyntaxSection (new ControlSection)
        s.run()
        s.render()
    }
}
