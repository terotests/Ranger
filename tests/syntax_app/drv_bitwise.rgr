; Driver: compiles and runs the `bitwise` section on its own, so that a target
; that rejects one section does not hide the others.
Import "sx_bitwise.rgr"

class BitwiseDriver {
    sfn m@(main):void () {
        def s:SyntaxSection (new BitwiseSection)
        s.run()
        s.render()
    }
}
