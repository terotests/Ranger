; Driver: compiles and runs the `arrays` section on its own, so that a target
; that rejects one section does not hide the others.
Import "sx_arrays.rgr"

class ArraysDriver {
    sfn m@(main):void () {
        def s:SyntaxSection (new ArraysSection)
        s.run()
        s.render()
    }
}
