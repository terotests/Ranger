; Driver: compiles and runs the `maps` section on its own, so that a target
; that rejects one section does not hide the others.
Import "sx_maps.rgr"

class MapsDriver {
    sfn m@(main):void () {
        def s:SyntaxSection (new MapsSection)
        s.run()
        s.render()
    }
}
