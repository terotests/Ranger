; Test: Array Push - Basic array operations
; Expected: Should compile and run successfully

class Test_ArrayPush {
    sfn m@(main):void () {
        def items:[string]
        push items "hello"
        push items "world"
        print "Done"
    }
}
