; LLVM PoC: factorial with while loop (mutable locals)

class Fact {
    sfn factorial:int (n:int) {
        def result:int 1
        def i:int n
        while (> i 1) {
            (= result (* result i))
            (= i (- i 1))
        }
        return result
    }
}
